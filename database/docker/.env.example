# EVALIS PostgreSQL Docker Environment Variables
# Copy this file to .env and customize as needed for your environment
# Do NOT commit .env to version control (it contains passwords)

# PostgreSQL Configuration
POSTGRES_DB=evalis_db
POSTGRES_USER=evalis_user
POSTGRES_PASSWORD=evalis2024

# Locale Configuration (Catalan)
LANG=ca_ES.UTF-8
LC_COLLATE=ca_ES.UTF-8
LC_CTYPE=ca_ES.UTF-8

# PostgreSQL Data Directory
PGDATA=/var/lib/postgresql/data/pgdata

# Port mapping (host:container)
# POSTGRES_PORT=5432

# Connection timeout (seconds)
# POSTGRES_CONNECT_TIMEOUT=10

# Development vs Production
# ENVIRONMENT=development  # or production
